import { FileCheck2,Globe2,Menu,X } from 'lucide-react'
import { useState } from 'react'
import { Link,NavLink } from 'react-router-dom'
import { useAuth } from '../lib/auth'
import { useLanguage } from '../lib/i18n'
export function Logo(){return <Link className="logo" to="/"><span className="logo-mark"><FileCheck2 size={19}/></span><span>Baakanya</span></Link>}
export default function Layout({children}){const[open,setOpen]=useState(false);const{user,signOut}=useAuth();const{language,toggle,t}=useLanguage();const close=()=>setOpen(false);return <div className="site"><header className="nav-wrap"><nav className="nav container"><Logo/><button className="menu-btn" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button><div className={`nav-links ${open?'open':''}`}><a href="#tools" onClick={close}>{t.tools}</a><a href="#how" onClick={close}>{t.how}</a><a href="#pricing" onClick={close}>{t.pricing}</a><button className="language" onClick={toggle}><Globe2 size={16}/>{language==='en'?'Setswana':'English'}</button>{user?<><NavLink to="/workspace" onClick={close}>{t.dashboard}</NavLink><button className="btn btn-small btn-ink" onClick={signOut}>Sign out</button></>:<><NavLink to="/auth" onClick={close}>{t.login}</NavLink><NavLink className="btn btn-small btn-blue" to="/auth?mode=signup" onClick={close}>{t.start}</NavLink></>}</div></nav></header><main>{children}</main><footer><div className="container footer-grid"><div><Logo/><p>Documents, sorted. Botswana-style.</p></div><div><b>Product</b><Link to="/workspace">Document tools</Link><a href="#pricing">Pricing</a></div><div><b>Support</b><a href="mailto:hello@baakanya.co.bw">hello@baakanya.co.bw</a><span>Gaborone, Botswana</span></div></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} Baakanya</span><span>Made with botho in Botswana 🇧🇼</span></div></footer></div>}
