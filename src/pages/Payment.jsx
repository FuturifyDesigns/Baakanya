import { UploadCloud } from 'lucide-react'
import { useState } from 'react'
import { useSearchParams } from 'react-router-dom'
import Layout from '../components/Layout'
import { useAuth } from '../lib/auth'
import { supabase } from '../lib/supabase'
export default function Payment(){const[params]=useSearchParams();const[plan,setPlan]=useState(params.get('plan')==='credits'?'credits':'subscription');const[receipt,setReceipt]=useState(null);const[message,setMessage]=useState('');const{user}=useAuth();const bank={name:import.meta.env.VITE_BANK_NAME||'Bank details pending',account:import.meta.env.VITE_BANK_ACCOUNT_NAME||'Baakanya',number:import.meta.env.VITE_BANK_ACCOUNT_NUMBER||'—',branch:import.meta.env.VITE_BANK_BRANCH_CODE||'—'};const submit=async()=>{if(!user||!supabase){setMessage('Sign in to submit a receipt once Supabase is connected.');return}if(!receipt){setMessage('Choose a receipt image first.');return}const path=`${user.id}/${crypto.randomUUID()}-${receipt.name}`;const upload=await supabase.storage.from('payment-receipts').upload(path,receipt);if(upload.error){setMessage(upload.error.message);return}const amount=plan==='credits'?25:40;const result=await supabase.from('payment_submissions').insert({user_id:user.id,amount,plan_type:plan,receipt_image_path:path});setMessage(result.error?result.error.message:'Receipt submitted. We will review it and update your access.')};return <Layout><section className="payment-page container"><span className="kicker">MANUAL PAYMENT</span><h1>Choose access that fits.</h1><div className="payment-grid"><div className="form-card"><div className="plan-select"><button className={plan==='credits'?'active':''} onClick={()=>setPlan('credits')}><b>P25 once-off</b><span>5 credits · no expiry</span></button><button className={plan==='subscription'?'active':''} onClick={()=>setPlan('subscription')}><b>P40 monthly</b><span>Unlimited documents</span></button></div><h3>1. Make a bank payment</h3><dl className="bank-details"><div><dt>Bank</dt><dd>{bank.name}</dd></div><div><dt>Account name</dt><dd>{bank.account}</dd></div><div><dt>Account number</dt><dd>{bank.number}</dd></div><div><dt>Branch code</dt><dd>{bank.branch}</dd></div><div><dt>Amount</dt><dd>P{plan==='credits'?'25':'40'}</dd></div></dl><h3>2. Upload proof of payment</h3><label className="receipt-upload"><input type="file" accept="image/*,.pdf" onChange={e=>setReceipt(e.target.files[0])}/><UploadCloud/><span>{receipt?receipt.name:'Choose receipt image or PDF'}</span></label><button className="btn btn-blue" onClick={submit}>Submit for review</button>{message&&<div className="form-message">{message}</div>}</div><aside className="payment-aside"><h2>What happens next?</h2><ol><li><span>1</span>We receive your proof of payment.</li><li><span>2</span>A Baakanya admin verifies the payment.</li><li><span>3</span>Your credits or monthly access are activated.</li></ol><p>Most payments are reviewed during Botswana business hours.</p></aside></div></section></Layout>}
