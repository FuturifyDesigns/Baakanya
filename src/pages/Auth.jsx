import { useEffect,useState } from 'react'
import { Link,useNavigate,useSearchParams } from 'react-router-dom'
import { CheckCircle2 } from 'lucide-react'
import { Logo } from '../components/Layout'
import { supabase } from '../lib/supabase'
import { useAuth } from '../lib/auth'
export default function Auth(){const[params]=useSearchParams();const[signup,setSignup]=useState(params.get('mode')==='signup');const[form,setForm]=useState({name:'',email:'',password:''});const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);const{configured,user}=useAuth();const navigate=useNavigate();useEffect(()=>{if(user)navigate('/workspace')},[user,navigate]);const submit=async e=>{e.preventDefault();if(!supabase){setMessage('Supabase is not connected yet. You can still explore every tool in preview mode.');return}setBusy(true);setMessage('');const result=signup?await supabase.auth.signUp({email:form.email.trim().toLowerCase(),password:form.password,options:{data:{name:form.name}}}):await supabase.auth.signInWithPassword({email:form.email.trim().toLowerCase(),password:form.password});setBusy(false);if(result.error)setMessage(result.error.message);else if(signup)setMessage('Check your email to verify your account and activate your 7-day trial.');else navigate('/workspace')};return <div className="auth-page"><div className="auth-brand"><Logo/><div><span className="kicker light">DOCUMENTS, SORTED.</span><h1>A small toolkit for the admin that moves life forward.</h1><ul><li><CheckCircle2/>All tools free for seven days</li><li><CheckCircle2/>No card required</li><li><CheckCircle2/>Botswana-ready templates</li></ul></div><p>Made with botho in Botswana 🇧🇼</p></div><div className="auth-form-wrap"><Link to="/" className="auth-back">← Back to home</Link><form className="auth-form" onSubmit={submit}><span className="kicker">{signup?'START FREE':'WELCOME BACK'}</span><h2>{signup?'Create your account':'Log in to Baakanya'}</h2><p>{signup?'Seven days of full access. No payment upfront.':'Pick up where you left off.'}</p>{signup&&<label>Full name<input required value={form.name} onChange={e=>setForm(x=>({...x,name:e.target.value}))} placeholder="Your full name"/></label>}<label>Email address<input required type="email" value={form.email} onChange={e=>setForm(x=>({...x,email:e.target.value}))} placeholder="you@example.com"/></label><label>Password<input required minLength="8" type="password" value={form.password} onChange={e=>setForm(x=>({...x,password:e.target.value}))} placeholder="At least 8 characters"/></label><button className="btn btn-blue" disabled={busy}>{busy?'Please wait…':signup?'Start my free trial':'Log in'}</button>{message&&<div className="form-message">{message}</div>}{!configured&&<Link className="demo-link" to="/workspace">Continue in preview mode →</Link>}<p className="switch-auth">{signup?'Already have an account?':'New to Baakanya?'} <button type="button" onClick={()=>{setSignup(!signup);setMessage('')}}>{signup?'Log in':'Start free'}</button></p></form></div></div>}
