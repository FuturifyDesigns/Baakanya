import { useEffect,useState } from 'react'
import { useAuth } from './auth'
import { supabase } from './supabase'
export function useAccess(){const{configured,user,loading:authLoading}=useAuth();const[state,setState]=useState({loading:configured,allowed:!configured,reason:configured?'Checking access…':'Preview mode'});useEffect(()=>{let active=true;if(!configured){setState({loading:false,allowed:true,reason:'Preview mode'});return}if(authLoading)return;if(!user){setState({loading:false,allowed:false,reason:'Sign in to use this tool'});return}(async()=>{const now=new Date();const[profileResult,subscriptionResult,creditResult]=await Promise.all([supabase.from('profiles').select('trial_end_date').eq('id',user.id).maybeSingle(),supabase.from('subscriptions').select('end_date,status').eq('user_id',user.id).eq('status','active').order('end_date',{ascending:false}).limit(1).maybeSingle(),supabase.from('credits').select('balance').eq('user_id',user.id).maybeSingle()]);if(!active)return;const trialActive=profileResult.data?.trial_end_date&&new Date(profileResult.data.trial_end_date)>now;const subscriptionActive=subscriptionResult.data?.end_date&&new Date(subscriptionResult.data.end_date)>now;const creditActive=(creditResult.data?.balance||0)>0;setState({loading:false,allowed:Boolean(trialActive||subscriptionActive||creditActive),reason:trialActive?'Free trial active':subscriptionActive?'Monthly access active':creditActive?`${creditResult.data.balance} credits available`:'Your free trial has ended'})})().catch(()=>active&&setState({loading:false,allowed:false,reason:'Access could not be verified'}));return()=>{active=false}},[configured,user,authLoading]);return state}
